
# Tuples 

    # Armazenar mais de uma informação em variáveis
    # Manter a sequencia dos dados em uma variável
    # não pode ser alteradas (imutable)

cores_list = ['amrelo', 'verde', 'azul', 'vermelho']

# representação direta para tuple
cores_tuple = ('amrelo', 'verde', 'azul', 'vermelho')

print(type(cores_list));
print(type(cores_tuple))

# Listas gastam mais memórias que as Tuples mesmo que a quantidade de itens sejam iguais
# Tuple possuem resultado melhor no desempenho